import React from 'react';
import {OnboardingSlideScreen} from '../../components/Onboarding';

export const OnboardingScreen = ({navigation}) => {
  return <OnboardingSlideScreen navigation={navigation} />;
};
